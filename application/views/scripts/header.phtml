<?php
global $st_lang;
$settingsUrl = Zend_Registry::get('config')->moxie->settings->url;

$url = $_SERVER['REQUEST_URI'];
$st_urls = array(
		'incomes' => '/incomes',
		'expenses' => '/expenses',
		'stats' => '/stats',
		'sheets' => '/sheets',
		'finance' => '/finance',
);

if (empty($_SESSION['user_id'])) {
?>
<header>
<table width=100%>
<tr>
<td valign="top" class="moxietitle moxietitle_mobile_not_logged" onclick="window.location='<?php echo $settingsUrl; ?>'"></td>
<td align="right" class="moxie_login_header">
</td>
</tr>
</table>
</header>
<?php
}
?>
<!-- web menu -->
<nav class="navbar navbar-default navbar-static-top" role="navigation">
	<div class="container">

        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>

		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
			<ul class="nav navbar-nav nav-pills">
<?php
$st_lang['users'] = $st_lang['users_title'];
$s_webMenu = '';
if (isset($_SESSION['user_id'])) {
	$s_webMenu .= '
	<li role="presentation"><a href="'.$settingsUrl.'/users">'.$_SESSION['user_name'].'</a></li>';

	foreach($st_urls as $key => $value) {
		$class = (strpos($url, $value) !== false) ? 'class="active"' : '';
		$s_webMenu .= '<li role="presentation" '.$class.'>'
		.'<a href="'.$settingsUrl.$st_urls[$key].'">'.$st_lang[$key].'</a></li>';
	}
	$s_webMenu .= '<li role="presentation"><a href="'.$settingsUrl.'/login/logout">'.$st_lang['logout'].'</a></li>';
}
else {
	$s_webMenu .= '
	<li role="presentation"><a href="'.$settingsUrl.'/">'.$st_lang['index'].'</a></li>
	<li role="presentation"><a href="/finance/index">'.$st_lang['finance'].'</a></li>
	<li role="presentation"><a href="'.$settingsUrl.'/texts/about">'.$st_lang['about'].'</a></li>
	';
}
echo $s_webMenu;
?>
			</ul>
		</div>
	</div>
</nav>